import { WrapMarkJsDirective } from './wrap-mark-js.directive';

describe('WrapMarkJsDirective', () => {
  it('should create an instance', () => {
    const directive = new WrapMarkJsDirective();
    expect(directive).toBeTruthy();
  });
});
